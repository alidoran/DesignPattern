<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.design_pattern_android_java">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Adapter.AdapterMain"></activity>
        <activity android:name=".ProtoType.MainPrototype" />
        <activity android:name=".Builder.MainBuilderActivity" />
        <activity android:name=".FactoryMethod.MainFactoryMethodActivity" />
        <activity android:name=".SimpleFactory.Main_SimpleFactory" />
        <activity android:name=".Singleton.SingletonMain" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>